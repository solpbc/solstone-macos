public enum UICopy {
    public static let JOURNAL_MODE_THIS_MAC_LABEL = "this Mac"
    public static let JOURNAL_MODE_ANOTHER_MACHINE_LABEL = "another device"
    public static let JOURNAL_MODE_THIS_MAC_TRADEOFF = "everything runs on this Mac and your journal lives here. recommended if you're just getting started."
    public static let JOURNAL_MODE_ANOTHER_MACHINE_TRADEOFF = "this Mac syncs to a journal that lives on another device — your other Mac, your home server, or a journal you've been invited to."
    public static let PAIRING_NOTENTITLED_RECOVERY = "your journal is paired, but it isn't on the paid plan — so it can't sync over the internet. on the same wi-fi as your journal, or over your own vpn, it connects directly without the plan."
    public static let PAIRING_DISCONNECT_CONFIRM = "disconnect this Mac from your journal? your journal keeps everything — you can pair again anytime."
    public static let JOURNAL_MARK_CONFIRM_QUESTION = "does this match your journal?"
    public static let JOURNAL_MARK_CONFIRM_SUBTEXT = "your journal shows this same mark in its network app. it should match — exactly."
    public static let JOURNAL_MARK_CONFIRM_BUTTON = "yes — this is my journal"
    public static let JOURNAL_MARK_MISMATCH_BUTTON = "that doesn't match"
    public static let JOURNAL_MARK_CONFIRMED_LINE = "this is your journal"
    public static let JOURNAL_MARK_CONNECTING = "connecting…"
    public static let JOURNAL_MARK_MISMATCH_TITLE = "not connected"
    public static let JOURNAL_MARK_MISMATCH_BODY = "you said this mark doesn't match the one your journal shows — so we didn't connect this Mac. you may have pasted the wrong link, or something isn't right. try again, or reach us and we'll help."
    public static let JOURNAL_MARK_MISMATCH_FRESH_LINK = "get a fresh link"
    public static let JOURNAL_MARK_MISMATCH_SUPPORT = "email support@solstone.app"
    public static let SETTINGS_PREREQ_PERMISSIONS = "you'll also need to grant permissions →"
    public static let SETTINGS_NEXT_CONNECT_JOURNAL = "next: connect your journal →"
    public static let SETTINGS_NEXT_CHECK_STATUS = "next: check status →"
    public static let SETTINGS_TAB_DONE_A11Y = "configured"
    public static let SETTINGS_TAB_ATTENTION_A11Y = "needs attention"
    public static let SETTINGS_TAB_UPDATES_DONE_A11Y = "sol is up to date"
    public static let SETTINGS_ATTENTION_PERMISSIONS = "permissions needed"
    public static let SETTINGS_ATTENTION_JOURNAL = "journal setup needed"
    public static let SETTINGS_ATTENTION_UPDATE_AVAILABLE = "update available"
    public static let SETTINGS_ATTENTION_UPDATE_CHECK_FAILED = "update check failed"
    public static let SETTINGS_RESTART_REQUIRED_BANNER = "restart needed for this to take effect — restart now →"
    public static let JOURNAL_SETUP_NEEDED_OPEN_SETTINGS = "journal setup needed — open settings"
    public static let JOURNAL_NEEDS_ATTENTION_OPEN_SETTINGS = "journal needs attention — open settings"
    public static let JOURNAL_RESTARTING = "journal restarting…"
    public static let RESTART_JOURNAL = "restart journal"
    public static let STOP_JOURNAL = "stop journal"
    public static let START_JOURNAL = "start journal"
    public static let JOURNAL_STATUS_RUNNING = "running"
    public static let JOURNAL_STATUS_RESTARTING = "restarting…"
    public static let JOURNAL_STATUS_SETUP_NEEDED = "setup needed"
    public static let DOCTOR_SETUP_NEEDED_TITLE = "can't find the journal runtime"
    public static let DOCTOR_SETUP_NEEDED_DETAIL = "the installed journal couldn't be located — try again, or reinstall from settings"
    public static let DOCTOR_CHECK_FAILED_TITLE = "the health check couldn't finish"
    public static let JOURNAL_STATUS_NEEDS_ATTENTION = "needs attention"
    public static let JOURNAL_STATUS_STOPPED = "stopped"
    public static let JOURNAL_WAITING_FOR_READINESS_MENU = "on, waiting for journal"
    public static let JOURNAL_WAITING_FOR_READINESS_MENU_BUTTON = "on, waiting for journal →"
    public static let JOURNAL_WAITING_FOR_READINESS = "waiting for journal to be ready"
    public static let MENUBAR_PERMISSIONS_OPEN_SETTINGS = "permissions needed — open settings →"
    public static let MENUBAR_OBSERVING_CONNECTED = "on, connected"
    public static let MENUBAR_OBSERVATION_WEDGE_OPEN_SETTINGS = "needs attention — open settings →"
    public static let MENUBAR_OBSERVING_BUNDLED = "on · your journal on this Mac"
    public static let MENUBAR_OBSERVING_OFFLINE_SAVED_LOCALLY = "on, offline (saved locally) →"
    public static let MENUBAR_LOCAL_ONLY_SETUP_JOURNAL = "on — set up your journal →"
    public static let MENUBAR_SYNC_PAUSED = "on, sync paused"
    public static let MENUBAR_JOURNAL_STOPPED_BY_USER = "on — your journal is stopped →"
    public static let MENUBAR_JOURNAL_WAITING = "waiting for journal…"
    public static let MENUBAR_STARTING = "starting…"
    public static let MENUBAR_A11Y_PERMISSIONS_NEEDED = "sol — permissions needed"
    public static let MENUBAR_A11Y_NEEDS_ATTENTION = "sol — needs attention"
    public static let MENUBAR_A11Y_ERROR = "sol — error"
    public static let MENUBAR_A11Y_STARTING = "sol — starting"
    public static let MENUBAR_A11Y_JOURNAL_SETUP_NEEDED = "sol — journal setup needed"
    public static let MENUBAR_A11Y_JOURNAL_RESTARTING = "sol — journal restarting"
    public static let MENUBAR_A11Y_JOURNAL_NEEDS_ATTENTION = "sol — journal needs attention"
    public static let MENUBAR_A11Y_JOURNAL_NOT_RUNNING = "sol — journal not running"
    public static let MENUBAR_A11Y_WAITING_FOR_JOURNAL = "sol — waiting for journal"
    public static let MENUBAR_A11Y_OBSERVING_SYNC_PAUSED = "sol — on, sync paused"
    public static let MENUBAR_A11Y_OBSERVING_SAVED_LOCALLY = "sol — on, saved locally"
    public static let MENUBAR_A11Y_PAUSED = "sol — paused"
    public static let MENUBAR_A11Y_OBSERVING_BUNDLED = "sol — on · your journal on this Mac"
    public static let MENUBAR_A11Y_OBSERVING_CONNECTED = "sol — on, connected"
    public static let SETTINGS_OBSERVATION_OBSERVING = "on"
    public static let SETTINGS_OBSERVATION_PAUSED = "paused"
    public static let SETTINGS_OBSERVATION_STARTING = "starting…"
    public static let SETTINGS_OBSERVATION_ERROR = "needs attention"
    public static let SETTINGS_HELP_ICON_FULL = "on — your data's reaching your journal"
    public static let SETTINGS_HELP_ICON_HALF = "on — saved on this Mac, not reaching your journal right now"
    public static let SETTINGS_HELP_ICON_PAUSED = "paused"
    public static let SETTINGS_HELP_ICON_ERROR = "needs your attention — permissions or a problem"
    public static let JOURNAL_CHILD_BREAKER_TRIPPED = "journal stopped after repeated exits"
    public static let JOURNAL_MATERIALIZE_FAILED = "journal runtime couldn't be prepared"
    public static let JOURNAL_SPAWN_BLOCKED_PORTS = "journal ports are still in use"
    public static let JOURNAL_SPAWN_PORT_CHECK_FAILED = "couldn't verify journal ports are free"
    public static let JOURNAL_SPAWN_FAILED = "journal couldn't start"
    public static let JOURNAL_READINESS_TIMEOUT = "journal didn't become ready in time"
    public static let JOURNAL_SETUP_NEEDED_BEFORE_UPGRADE = "journal setup needed before upgrade can continue"
    public static let INSTALLER_INLINE_FAILURE_GENERIC = "setup hit a snag — retry below, or open details to share with support."
    public static let INSTALLER_READINESS_TIMEOUT = "the journal didn't become ready in time"
    public static let INSTALLER_READINESS_GATE_FAILED = "couldn't get the journal ready for this Mac"
    public static let INSTALLER_VERIFY_INTEGRITY_LABEL = "verifying macOS security integrity"
    public static let ERROR_LOGIN_ITEM = "couldn't update your login setting. try again, or toggle it off and on."
    public static let ERROR_SAVE_CONFIG = "couldn't save your settings. try again, or restart sol."
    public static let ERROR_START_OBSERVING = "couldn't turn sol on. check permissions in settings, then try again."

    public static func menubarErrorOpenSettings(_ message: String) -> String {
        "error: \(message) →"
    }

    public static func installerVerifyIntegrityWarning(library: String) -> String {
        "couldn't get \(library) ready; continuing"
    }
}
